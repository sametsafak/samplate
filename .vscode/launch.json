{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "gulp watch",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}/src"
      // "sourceMapPathOverrides": {
      //   "/iyidir/*": "${workspaceFolder}/naber/*"
      // }
    }
  ]
}